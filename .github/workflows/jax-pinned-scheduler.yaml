name: jax-pinned-scheduler

on:
  schedule:
    - cron: '0 6 * * 1,3,5'
  workflow_dispatch:

jobs:
  run-tpu-single-slice-training-test:
    runs-on: ubuntu-latest
    if: always()
    permissions:
      actions: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Run JAX Pinned Sequence Training Tests
        env: 
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          echo "Triggering workflow: tpu-single-slice-training-test.yaml"
          gh workflow run tpu-single-slice-training-test.yaml --ref jax_0.10.2

          echo "Waiting 30 seconds for the new run to be registered with the API..."
          sleep 30

          echo "Fetching the ID of the new run..."
          RUN_ID=$(gh run list --workflow="tpu-single-slice-training-test.yaml" --branch jax_0.10.2 --limit 1 --json databaseId -q '.[0].databaseId')

          if [ -z "$RUN_ID" ]; then
            echo "Error: Could not find a recent run for the workflow."
            exit 1
          fi

          echo "Found run ID: $RUN_ID. Waiting for it to complete... every 3 mins"
          gh run watch "$RUN_ID" --interval 180
  
  run-tpu-multi-slices-training-test:
    runs-on: ubuntu-latest
    needs: run-tpu-single-slice-training-test
    if: always()
    permissions:
      actions: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Run JAX Pinned Sequence Training Tests
        env: 
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          echo "Triggering workflow: tpu-multislice-training-test.yaml"
          gh workflow run tpu-multislice-training-test.yaml --ref jax_0.10.2

          echo "Waiting 30 seconds for the new run to be registered with the API..."
          sleep 30

          echo "Fetching the ID of the new run..."
          RUN_ID=$(gh run list --workflow="tpu-multislice-training-test.yaml" --branch jax_0.10.2 --limit 1 --json databaseId -q '.[0].databaseId')

          if [ -z "$RUN_ID" ]; then
            echo "Error: Could not find a recent run for the workflow."
            exit 1
          fi

          echo "Found run ID: $RUN_ID. Waiting for it to complete... every 3 mins"
          gh run watch "$RUN_ID" --interval 180

  run-pw-single-slice-training-test:
    runs-on: ubuntu-latest
    needs: run-tpu-multi-slices-training-test
    if: always()
    permissions:
      actions: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Run JAX Pinned Sequence Training Tests
        env: 
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          echo "Triggering workflow: pw-single-slice-training-test.yaml"
          gh workflow run pw-single-slice-training-test.yaml --ref jax_0.10.2

          echo "Waiting 30 seconds for the new run to be registered with the API..."
          sleep 30

          echo "Fetching the ID of the new run..."
          RUN_ID=$(gh run list --workflow="pw-single-slice-training-test.yaml" --branch jax_0.10.2 --limit 1 --json databaseId -q '.[0].databaseId')

          if [ -z "$RUN_ID" ]; then
            echo "Error: Could not find a recent run for the workflow."
            exit 1
          fi

          echo "Found run ID: $RUN_ID. Waiting for it to complete... every 3 mins"
          gh run watch "$RUN_ID" --interval 180
  
  run-pw-multislice-training-test:
    runs-on: ubuntu-latest
    needs: run-pw-single-slice-training-test
    if: always()
    permissions:
      actions: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Run JAX Pinned Sequence Training Tests
        env: 
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          echo "Triggering workflow: pw-multislice-training-test.yaml"
          gh workflow run pw-multislice-training-test.yaml --ref jax_0.10.2

          echo "Waiting 30 seconds for the new run to be registered with the API..."
          sleep 30

          echo "Fetching the ID of the new run..."
          RUN_ID=$(gh run list --workflow="pw-multislice-training-test.yaml" --branch jax_0.10.2 --limit 1 --json databaseId -q '.[0].databaseId')

          if [ -z "$RUN_ID" ]; then
            echo "Error: Could not find a recent run for the workflow."
            exit 1
          fi

          echo "Found run ID: $RUN_ID. Waiting for it to complete... every 3 mins"
          gh run watch "$RUN_ID" --interval 180

  run-bazel-unit-tests:
    runs-on: ubuntu-latest
    if: always()
    permissions:
      actions: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Run Unit Tests on Bazel and CPU
        env: 
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          echo "Triggering workflow: bazel-unit-tests.yaml"
          gh workflow run bazel-unit-tests.yaml --ref jax_0.10.2

          echo "Waiting 30 seconds for the new run to be registered with the API..."
          sleep 30

          echo "Fetching the ID of the new run..."
          RUN_ID=$(gh run list --workflow="bazel-unit-tests.yaml" --branch jax_0.10.2 --limit 1 --json databaseId -q '.[0].databaseId')

          if [ -z "$RUN_ID" ]; then
            echo "Error: Could not find a recent run for the workflow."
            exit 1
          fi

          echo "Found run ID: $RUN_ID. Waiting for it to complete... every 3 mins"
          gh run watch "$RUN_ID" --interval 180
